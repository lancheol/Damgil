# Expo HAS CHANGED

Read the exact versioned docs at https://docs.expo.dev/versions/v57.0.0/ before writing any code.

App Store Expo Go currently aligns with SDK 57. Keep this project on SDK 57 for physical iPhone testing via Expo Go.

# Backend API

Before any server integration, follow `.cursor/rules/backend-api.mdc`. Base URL is `https://dam-gil.duckdns.org` (`/api/v1`). Swagger at `/docs` is the contract. Do not invent endpoints or commit test credentials.
